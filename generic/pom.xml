<?xml version="1.0"?>
<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<groupId>org.msh</groupId>
		<artifactId>etbmanager</artifactId>
		<version>1.0.0</version>
	</parent>

	<groupId>org.msh</groupId>
	<artifactId>generic</artifactId>
	<version>1.0.0</version>
	<packaging>war</packaging>
	
	<properties>
		<workspace-context></workspace-context>
	</properties>

	<name>eTB Manager generic version ${project.version}</name>
	<url>http://www.etbmanager.org</url>


	<build>
		<finalName>etbmanager</finalName>

		<resources>
			<!-- will filter just the persistence.xml -->
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>META-INF/persistence.xml</include>
				</includes>
			</resource>
			<!-- Will include all other resources -->
			<resource>
				<directory>src/main/resources</directory>
				<excludes>
					<exclude>META-INF/persistence.xml</exclude>
				</excludes>
			</resource>
		</resources>

		<plugins>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>1.1</version>
				<executions>
					<execution>
						<id>add-source</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>src/main/java</source>
								<source>src/reports/java</source>
								<source>src/client/java</source>
								<source>src/custom/na</source>
								<source>src/custom/uz</source>
								<source>src/custom/vi</source>
								<source>src/custom/kh</source>
								<source>src/custom/bd</source>
								<source>src/custom/md</source>
								<source>src/custom/ng</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>2.4</version>
				<configuration>
					<webResources>
						<resource>
							<filtering>true</filtering>
							<directory>src/main/webapp</directory>
							<includes>
								<include>**/web.xml</include>
								<include>**/components.xml</include>
								<!-- include any other file types you want to filter -->
							</includes>
						</resource>
					</webResources>
					<packagingExcludes>WEB-INF/deploy/**</packagingExcludes>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
